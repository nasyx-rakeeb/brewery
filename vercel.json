{
  "version": 2,
  "builds": [
    {
      "src": "src/index.mjs",  // Update the file extension here
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.mjs"  // Update the file extension here as well
    }
  ]
}
