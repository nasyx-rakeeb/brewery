{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",  // Update the file extension here
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.js"  // Update the file extension here as well
    }
  ]
}
